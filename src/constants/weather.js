export const WeatherSelectStyles={
    control:(base)=>({
        ...base,
        minWidth: '400px',
        maxWidth: 'auto',
    }),
    menu:(base)=>({
        ...base,
        minWidth: '400px',
        maxWidth: 'auto',
    })
}